import React from 'react'
import {  } from '@material-ui/core'
import { makeStyles } from '@material-ui/styles'

const BirthdayCards = () => {
    const classes = style();
    return (
        <div>
            this is birthdaycard
        </div>
    )
}

const style = makeStyles({

});

export default BirthdayCards;